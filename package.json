{
  "name": "agent-rpg",
  "private": true,
  "version": "1.0.0",
  "description": "Theme and agent tooling for Agent RPG",
  "scripts": {
    "lint:themes": "tsx scripts/lint-theme-aliases.ts",
    "lint:themes:strict": "tsx scripts/lint-theme-aliases.ts --strict",
    "list:themes": "tsx scripts/list-themes.ts",
    "agents:check": "tsx scripts/check-agents.ts",
    "ops:check": "npm run lint:themes:strict && npm run categories:check && npm run agents:check && npm run readme:check",
    "readme:themes": "tsx scripts/update-readme-theme-table.ts",
    "readme:check": "tsx scripts/check-readme-theme-sync.ts",
    "agents:check:strict": "tsx scripts/check-agents.ts --strict",
    "ops:fix": "npm run readme:themes && npm run lint:themes && npm run categories:check && npm run agents:check",
    "categories:check": "tsx scripts/check-theme-categories.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "toml": "^3.0.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
